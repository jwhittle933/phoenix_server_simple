<h2> Topics </h2>

<ul class="container">
    <%= for topic <- @topics do %>
        <li>
            <%= topic.title %>
            <%= link "Edit", to: topic_path(@conn, :edit, topic), class: "link" %>
            <%= link "Delete", to: topic_path(@conn, :delete, topic), method: :delete, lass: "link"%>
        </li>
    <% end %>
</ul>

<div class="container">
    <%= link to: topic_path(@conn, :new), class: "btn btn-success" do %>
        New Item
    <% end %>
</div>